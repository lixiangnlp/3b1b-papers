"""《Attention Is All You Need》中文讲解旁白。

每个场景是一组 (key, 文本)。key 在全片内唯一，TTS 按 key 生成
build/audio/<key>.wav，动画里用 ``with self.voice(key):`` 与之同步。
修改文本后重新运行 TTS，只有内容变化的句子会被重新合成。
"""

NARRATION: dict[str, list[tuple[str, str]]] = {
    "S01_Intro": [
        ("intro_1",
         "2017年，谷歌的八位研究者发表了一篇只有十几页的论文，标题相当大胆："
         "Attention Is All You Need。注意力，就是你所需要的全部。"),
        ("intro_2",
         "今天的 GPT、BERT，以及几乎所有的大语言模型，骨架都来自这篇论文提出的架构，"
         "也就是 Transformer。GPT 里的那个 T，指的就是它。"),
        ("intro_3",
         "这期视频，我们就把这篇论文拆开来看：注意力机制到底在做什么，"
         "以及为什么它足以取代此前统治序列建模的循环神经网络。"),
    ],
    "S02_RNN": [
        ("rnn_1",
         "在 Transformer 之前，处理一句话最主流的方法是循环神经网络，也就是 RNN。"
         "它像一个人逐字阅读：读一个词，更新一次脑中的记忆，再去读下一个词。"),
        ("rnn_2",
         "这种做法有两个麻烦。第一，它天生是串行的：第五个词必须等前四个词都算完才能开始，"
         "GPU 上再多的计算核心，也只能在一旁干等。"),
        ("rnn_3",
         "第二，句首的信息要一步一步地传到句尾，每传一步都会被稀释一点。"
         "句子越长，相隔很远的两个词之间就越难建立联系。"),
        ("rnn_4",
         "Transformer 的想法很激进：干脆扔掉循环，让每个词直接看向句子里的所有其他词，一步到位。"
         "这个“看”的动作，就叫做注意力。"),
    ],
    "S03_Embedding": [
        ("emb_1",
         "不过在谈注意力之前，得先把文字变成计算机能处理的东西。"
         "第一步，是把每个词，更准确地说是每个 token，映射成一个高维向量，这叫做词嵌入。"),
        ("emb_2",
         "你可以把这些向量想象成空间中的箭头。在训练好的模型里，方向本身就带有含义："
         "意思相近的词，箭头也指向相近的方向。"),
        ("emb_3",
         "但问题是，刚查表得到的向量只知道这个词本身，不知道上下文。"
         "比如“苹果”，在“吃苹果”和“苹果手机”里意思完全不同，可它们的初始向量却一模一样。"),
        ("emb_4",
         "注意力机制要做的，就是让每个向量根据周围的词来调整自己，把上下文的信息吸收进来。"
         "在论文里，这些向量的维度是 512。"),
    ],
    "S04_QKV": [
        ("qkv_1",
         "我们用一个小例子：“一只毛茸茸的蓝色小猫”。"
         "假设名词“小猫”想知道一件事：前面有没有形容词在修饰我？"),
        ("qkv_2",
         "为此，每个词都把自己的向量乘以一个矩阵 W Q，得到一个查询向量，也就是 Query。"
         "你可以把它理解为这个词提出的问题：我在找什么？"),
        ("qkv_3",
         "同时，每个词还会乘以另一个矩阵 W K，得到一个键向量，也就是 Key。"
         "它相当于每个词对外宣称的标签：我是什么。"),
        ("qkv_4",
         "查询和键是否匹配，用点积来衡量：两个向量方向越一致，点积就越大。"
         "把每一对查询和键都算一遍，就得到了一张注意力分数表。"),
        ("qkv_5",
         "在我们的例子里，“小猫”的查询和“毛茸茸”、“蓝色”的键对得很好，分数很高；"
         "而和“的”这种虚词，分数就很低。"),
        ("qkv_6",
         "接下来，对每一列分数做 softmax，把它们变成加起来等于一的权重。"
         "分数高的，权重接近一；分数低的，被压到接近零。"),
        ("qkv_7",
         "最后还需要第三个矩阵 W V，它把每个词变成值向量，也就是 Value："
         "如果你关注我，我能提供给你的信息。"),
        ("qkv_8",
         "“小猫”用刚才的权重，把所有值向量加权求和，再加回到自己的向量上。"
         "于是它被推向了一个新的方向：不再只是“小猫”，而是“毛茸茸的蓝色小猫”。"),
    ],
    "S05_Formula": [
        ("formula_1",
         "把这一切写成矩阵形式，就是论文里最著名的那个公式：缩放点积注意力。"),
        ("formula_2",
         "Q 乘以 K 的转置，一次性算出所有查询和所有键的点积；softmax 把分数变成权重；"
         "再乘以 V，完成加权求和。整个过程只是几次矩阵乘法，非常适合在 GPU 上并行。"),
        ("formula_3",
         "那分母上的根号 d k 是做什么的？如果向量的每个分量都是均值为零、方差为一的随机数，"
         "那么两个 d k 维向量的点积，方差就是 d k。维度越高，分数的波动就越大。"),
        ("formula_4",
         "分数太大，softmax 就会变得极端，几乎所有的权重都挤到同一个位置上，梯度也随之消失。"
         "除以根号 d k，正好把方差拉回到一，让训练保持稳定。"),
    ],
    "S06_MultiHead": [
        ("multi_1",
         "一组 Q、K、V 只能捕捉一种关系。但语言里的关系多种多样："
         "代词要找到它指代的人，形容词要找到它修饰的名词，谓语要找到它的主语。"),
        ("multi_2",
         "所以论文让注意力并行地做八次，称为八个头。每个头都有自己独立的投影矩阵，"
         "把 512 维的向量投影到 64 维的子空间里，各自学习一种关注模式。"),
        ("multi_3",
         "八个头的输出拼接起来，重新得到 512 维，再乘以一个输出矩阵 W O 融合在一起。"
         "由于每个头的维度缩小了，多头注意力的总计算量和单头差不多。"),
    ],
    "S07_Position": [
        ("pos_1",
         "不过，注意力有一个奇怪的盲点：它根本不在乎词的顺序。"
         "把句子打乱，每个词得到的加权和完全不变。“狗咬人”和“人咬狗”，在它看来没有区别。"),
        ("pos_2",
         "论文的解法是位置编码：给每个位置生成一个同样是 512 维的向量，直接加到词嵌入上。"),
        ("pos_3",
         "这个向量由不同频率的正弦和余弦波组成。靠前的分量振荡得很快，靠后的分量变化得很慢，"
         "就像一组快慢不同的时钟指针，组合起来就能唯一地标识每一个位置。"),
        ("pos_4",
         "更妙的是，每一对正弦和余弦就是圆上的一个点，位置向后移动 k 步，只是把这个点旋转一个固定的角度。"
         "这是一个线性变换，让模型很容易学会按相对位置去关注。"),
    ],
    "S08_Architecture": [
        ("arch_1",
         "现在可以把零件组装起来了。Transformer 最初是为机器翻译设计的，"
         "分为左边的编码器和右边的解码器。"),
        ("arch_2",
         "编码器由六个相同的层堆叠而成。每层有两个子层：一个多头自注意力，一个逐位置的前馈网络。"
         "前馈网络先把每个向量扩展到 2048 维，经过 ReLU，再压回 512 维。"),
        ("arch_3",
         "每个子层外面都包着残差连接和层归一化，输出等于 LayerNorm 作用在 x 加上子层输出之上。"
         "残差让信息和梯度可以绕过子层直接流动，这是网络能够堆得很深的关键。"),
        ("arch_4",
         "解码器同样有六层，但多了一个子层：交叉注意力。它的查询来自解码器，键和值来自编码器的输出，"
         "让生成的每一个词都能回头查看原文。"),
        ("arch_5",
         "另外，解码器的自注意力需要加上掩码。生成第 t 个词的时候，不能偷看后面的答案，"
         "所以注意力矩阵的右上三角被设为负无穷，经过 softmax 之后就变成了零。"),
        ("arch_6",
         "最后，解码器的输出经过一个线性层和 softmax，变成下一个词的概率分布。"
         "模型一个词一个词地生成，直到输出句子的结束符。"),
    ],
    "S09_WhySelfAttention": [
        ("why_1",
         "论文专门比较了自注意力、循环层和卷积层。自注意力每层的计算量是 n 的平方乘以 d，"
         "循环层是 n 乘以 d 的平方。当句子长度 n 小于向量维度 d 时，自注意力反而更便宜。"),
        ("why_2",
         "更重要的是，自注意力的串行操作数，和任意两个词之间的路径长度，都只是常数；"
         "而循环网络需要 n 步。这正是它训练更快、也更擅长捕捉长距离依赖的原因。"),
        ("why_3",
         "当然，n 的平方也成了它后来的阿喀琉斯之踵：上下文越长，代价增长得越快。"
         "这催生了此后大量关于高效注意力的研究。"),
    ],
    "S10_Results": [
        ("res_1",
         "实验结果相当惊人。在 WMT 2014 英德翻译任务上，大号 Transformer 拿到了 28.4 的 BLEU 分数，"
         "比此前包括集成模型在内的最好成绩高出两分以上；在英法翻译上达到了 41.8。"),
        ("res_2",
         "而训练它只用了八块 P100 GPU、三天半的时间，只是此前最好模型训练成本的一小部分。"),
        ("res_3",
         "此后的故事大家都知道了：只用编码器的 BERT，只用解码器的 GPT，"
         "再到今天参数动辄千亿的大语言模型，它们的核心依然是这篇论文里的那几个矩阵乘法。"),
        ("res_4",
         "回过头看，标题里的那句话或许有些夸张。但在过去的这些年里，注意力，确实几乎就是我们所需要的全部。"
         "感谢收看，我们下期再见。"),
    ],
}

SCENE_ORDER = list(NARRATION)

NARRATION_BY_KEY: dict[str, str] = {
    key: text for lines in NARRATION.values() for key, text in lines
}

# 送进 TTS 前的读音替换：屏幕上显示的写法 -> 更容易读对的写法。
TTS_REPLACEMENTS: dict[str, str] = {
    "Attention Is All You Need": "Attention is all you need",
    "BLEU": "blue",
    "P100": "P一百",
    "WMT 2014": "WMT 二零一四",
    "2017年": "二零一七年",
    "LayerNorm": "Layer Norm",
    "ReLU": "ReLU",
}


def speakable(text: str) -> str:
    for src, dst in TTS_REPLACEMENTS.items():
        text = text.replace(src, dst)
    return text
